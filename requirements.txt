Django==5.1.15
djangorestframework==3.15.2
django-cors-headers==4.9.0
django-environ==0.11.2
psycopg[binary]==3.3.4

# Production WSGI server. Not used by `manage.py runserver`, and it does not
# run on Windows — deploy targets are Linux.
gunicorn==26.0.0
